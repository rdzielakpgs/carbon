import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";

import NavigationBar from "./navigation-bar.component";

<Meta
  title="Design System/Navigation Bar/Test"
  parameters={{
    info: { disable: true },
    chromatic: {
      disable: true,
    },
    knobs: {
      disable: true,
    },
  }}
/>

export const NavigationBarStory = (args) => <NavigationBar {...args} />;

# Navigation Bar

### Default

<Preview>
  <Story
    name="default"
    args={{
      children: "Example content",
    }}
  >
    {NavigationBarStory.bind({})}
  </Story>
</Preview>
